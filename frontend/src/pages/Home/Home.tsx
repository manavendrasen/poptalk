import { Container } from "@mui/material";
import React from "react";

interface HomeProps {}

export const Home: React.FC<HomeProps> = ({}) => {
  return (
    <Container>
      <h1>Home</h1>
    </Container>
  );
};
